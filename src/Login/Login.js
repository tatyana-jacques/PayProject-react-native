import {StatusBar, SafeAreaView, Text, TouchableOpacity } from "react-native"

export default function Login () {
    return (
        <SafeAreaView>
            <StatusBar/>
            <Text>Login</Text>
        </SafeAreaView>
    )
}