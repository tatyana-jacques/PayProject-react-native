import {StatusBar, SafeAreaView, Text, TouchableOpacity } from "react-native"

export default function Home () {
    return (
        <SafeAreaView>
            <StatusBar/>
            <Text>Home</Text>
        </SafeAreaView>
    )
}